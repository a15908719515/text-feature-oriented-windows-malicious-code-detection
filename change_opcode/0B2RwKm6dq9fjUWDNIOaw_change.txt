push mov sub mov push pop sub mov add xor or cmp je test je xor push pop sub lea push pop add cmp je test je push pop cmp je cmp je add sub add xor lea and add or xor add sub mov xor sub add mov xor test je lea or sub cmp je add push pop lea cmp je test je mov cmp je add push pop xor mov add push pop push pop push pop xor add xor lea sub add xor lea leave retn push mov sub xor or test je xor sub lea test je xor and xor test je xor and mov test je mov xor lea or push pop test je mov xor sub cmp je sub or xor test je xor add lea mov add mov xor push pop or xor add and sub push pop push pop or add and xor add xor test je mov lea sub cmp je test je xor add mov add and cmp je or cmp je push pop add or test je and lea xor or add sub xor lea sub add leave retn push mov sub mov add xor test je cmp je test je xor cmp je xor cmp je xor sub xor lea cmp je mov add sub add push pop lea push pop add and or and or push pop sub add xor sub test je lea xor lea sub xor push pop test je xor sub xor leave retn push mov sub call xor lea xor test je test je add or call xor sub cmp je add mov add xor sub cmp je cmp je cmp je lea push pop xor push pop xor lea mov xor push pop xor add xor leave retn push mov sub call xor add xor lea push xor push call mov xor sub xor lea mov sub xor test je test je test je sub lea mov xor sub add xor lea cmp je test je test je add xor mov add call leave retn push mov sub lea push call mov sub cmp je xor add push pop test je test je sub xor add mov xor sub xor add or push pop lea push call and lea cmp je mov push pop lea test je sub xor test je sub mov push pop and lea push pop add sub push pop test je and push pop add sub cmp je sub xor mov test je test je xor and lea xor add or add sub xor add xor lea push sub push call xor cmp je push pop lea push sub push call xor push pop lea cmp je add test je xor mov and xor push pop call or xor cmp je add xor or add lea test je test je lea mov xor add xor mov xor or sub add and lea sub xor lea push call push pop and test je or lea or test je push pop add xor and add lea test je push pop mov test je test je xor lea mov test je xor lea xor sub add or push pop xor or push pop push pop xor push call mov lea push call mov xor add lea sub cmp je test je cmp je xor sub lea xor lea push call xor sub xor cmp je xor add push pop test je push pop add sub mov sub lea mov or call xor push pop add cmp je xor lea add push pop test je and add xor mov and cmp je sub test je push pop add push pop cmp je sub xor cmp je xor add lea add mov or lea push sub push call and add mov sub lea push call xor push pop sub add cmp je test je mov sub call add mov xor test je test je test je sub add xor or lea xor lea push call leave retn push mov sub mov add push pop push pop and test je cmp je lea xor sub lea or add mov add xor sub add lea and push pop or sub cmp je push pop test je lea test je xor and test je mov push pop test je mov xor mov push pop add mov xor add sub xor add or mov xor mov xor sub xor cmp je push pop xor sub or cmp je xor cmp je lea push pop xor or sub xor mov lea mov lea mov add sub cmp je mov push pop push pop xor test je xor sub leave retn push mov sub call xor test je sub lea or test je xor add xor test je xor push pop and mov xor sub push pop mov push call mov push pop sub and sub test je cmp je test je lea xor lea push pop mov xor mov xor add cmp je push pop lea xor test je sub add push pop lea test je xor and xor sub add push pop xor sub add leave retn push mov sub lea push call lea push call cmp je xor lea add push pop test je lea mov cmp je xor sub xor lea sub or mov xor lea push call lea push call cmp je test je test je xor or add or lea push call xor lea push call sub mov leave retn push mov sub lea push and push call lea push call leave retn push mov sub call add xor add test je cmp je xor and xor lea push call lea push call add sub push call xor sub lea push call or leave retn push mov sub call push call xor lea push call add xor lea push xor push call xor leave retn push mov sub push call add push pop sub push pop test je lea xor lea push call xor mov add xor test je lea cmp je xor lea cmp je xor add xor lea add push pop add lea push pop add call push call mov add lea sub test je cmp je xor sub add xor push pop and mov lea test je test je test je xor push pop xor mov leave retn push mov sub push call test je sub lea mov add test je sub add test je xor mov lea sub push pop push call add test je sub xor test je push pop xor mov lea add lea push sub push call xor add test je cmp je add push pop sub lea or sub call test je test je xor sub xor add call mov xor test je cmp je sub xor call xor sub call push pop xor lea push sub push call push pop add xor leave retn push mov sub xor lea mov test je xor push pop sub xor lea push call lea push call mov xor test je sub or xor cmp je mov push pop xor sub xor mov lea push call mov push pop cmp je leave retn push mov sub lea push and push call push pop sub or xor sub test je xor lea call push pop push pop sub push pop sub test je sub push pop or sub test je add or lea add test je add xor and test je or mov xor lea push pop xor mov push pop xor and sub xor sub call push call sub or add cmp je test je push pop xor push pop xor push call cmp je lea xor test je push pop sub add and xor leave retn push mov sub push lea push lea push call add or add cmp je sub and test je and xor push pop or lea push call test je test je xor sub xor leave retn push mov sub add or sub cmp je mov sub mov xor lea xor push pop test je sub add xor test je cmp je sub xor lea mov sub push pop add xor push pop sub xor add test je cmp je test je xor and add lea mov and xor push pop sub test je cmp je test je or xor lea leave retn push mov sub lea push call push pop and mov xor sub cmp je mov lea and push pop leave retn push mov sub push call mov xor mov test je cmp je cmp je add or sub xor mov sub push call and test je lea push pop xor sub push pop test je lea mov xor push pop sub test je xor lea sub xor test je xor and test je xor add lea xor cmp je xor push pop or add xor push pop add xor mov sub or xor cmp je sub add lea push call leave retn push mov sub push lea push call xor test je lea or call sub test je add mov cmp je sub add xor and xor test je lea sub xor cmp je sub push pop push pop push pop push pop lea and or call xor lea call add xor sub mov and test je test je cmp je xor sub or mov sub leave retn push mov sub cmp je lea push pop lea add test je or sub push pop xor test je lea sub lea test je add xor mov cmp je lea xor sub mov sub xor add xor lea xor push pop xor and sub mov sub call mov push lea push call mov sub leave retn push mov sub xor push call push pop push call or and sub xor sub cmp je cmp je cmp je add push pop or push pop lea push pop call mov push call xor call test je test je test je xor sub xor push call test je add sub add test je cmp je sub xor sub lea sub push pop lea add lea sub push pop test je test je test je sub xor mov add xor test je add mov lea push call lea call add push pop or mov xor lea test je test je lea push pop xor call xor or xor cmp je test je test je xor mov sub and push pop add push call push pop sub xor cmp je mov xor cmp je cmp je xor mov lea test je add xor add push pop xor push pop or mov push pop push pop lea sub xor add sub test je add push pop sub add xor sub push call lea and push pop sub cmp je push pop lea call xor and add xor test je xor test je push pop xor cmp je push pop test je cmp je add or mov cmp je push pop add mov lea xor mov xor add or push pop mov push pop lea push pop lea xor sub push pop lea and lea push call xor mov lea mov test je test je and mov call xor sub xor test je test je xor and push call xor add and push call sub push pop lea sub push call xor mov push call xor lea push call sub xor push call lea xor push lea push lea push call push call and xor push pop xor push pop xor sub test je test je xor add xor push call test je sub test je push pop lea test je test je lea push pop sub cmp je sub or xor or push pop mov xor add push pop sub or call lea mov add cmp je xor add lea push pop add push lea push lea push call mov add lea push call or xor call lea sub and sub xor test je cmp je cmp je xor push pop mov push sub push call add sub cmp je xor sub xor test je and lea add test je or add lea sub xor cmp je xor cmp je xor mov lea test je xor and xor mov xor lea xor call test je sub push call push pop xor sub test je xor sub add test je and cmp je xor push pop mov test je sub xor cmp je sub push pop and xor lea push pop add sub xor and sub xor call add mov lea add and add sub test je test je test je sub and xor add mov xor test je mov cmp je push pop add sub add push call and push call add call sub xor test je sub add test je push pop sub mov xor mov call xor test je add cmp je push pop push pop mov sub mov sub add lea push call or lea push call test je mov add push pop cmp je xor test je sub push pop xor mov test je lea sub add test je lea xor mov xor push pop xor mov lea sub lea mov sub and push pop sub push pop sub cmp je test je sub add sub lea cmp je xor lea sub cmp je add mov push pop mov test je lea push pop lea add push pop lea push xor push call leave retn push mov sub push sub push call push call add cmp je lea xor sub xor test je lea add and sub mov cmp je test je test je push pop mov push pop test je lea add xor lea cmp je sub add lea xor and add lea add sub add mov lea add test je or cmp je xor and test je xor lea and xor test je lea mov lea xor or test je cmp je xor add mov add lea sub call mov or push pop test je and add push call sub or test je lea xor sub xor cmp je xor push pop mov and push pop or add lea push call push pop xor push lea push lea push call push pop push pop sub xor push call lea add xor mov or xor pop leave retn push mov sub call push lea push lea push call push lea push call sub add sub lea cmp je add push call sub push pop push pop add or test je push pop lea xor add xor call sub lea test je sub lea call push pop lea push call lea xor call add push pop push pop xor add test je test je test je xor call lea cmp je test je test je and xor sub mov and sub mov add test je test je xor push call cmp je lea push call lea add sub push pop cmp je sub xor lea test je sub xor test je and xor lea add sub xor push pop call or add xor sub mov cmp je lea xor cmp je add xor mov or xor sub push call xor add call xor sub add sub push lea push call and lea cmp je test je sub or xor sub and cmp je test je xor sub add xor mov test je xor test je mov add xor lea test je lea test je xor mov push pop mov sub mov add xor and xor sub xor add xor and add push pop add lea xor add push call xor push call xor and push call cmp je xor push pop xor test je xor push pop xor and lea test je test je or xor sub add xor add sub add xor push pop cmp je cmp je mov lea add xor mov xor sub mov add test je cmp je test je sub xor mov push pop push call lea push call test je test je push pop mov push pop test je xor cmp je xor test je add mov lea cmp je push pop lea and push pop push pop sub xor lea sub and add xor add xor push call mov push pop add push pop cmp je xor add sub push call test je sub xor add cmp je xor push pop mov xor push lea push call sub xor push call xor add lea call add xor lea push call lea xor sub xor test je cmp je lea sub xor call push pop or xor add or lea xor sub lea cmp je test je cmp je push pop or push pop or sub push call mov xor call xor mov add lea mov sub test je test je test je and xor or and lea push call and xor test je xor cmp je push pop sub xor sub add cmp je cmp je cmp je and mov sub xor push pop push pop xor sub add mov push pop or sub lea push and push call xor push call lea push pop push pop xor cmp je xor add test je mov push pop cmp je xor and cmp je sub push pop xor and add push pop lea xor push pop sub and or sub push call sub test je sub xor lea test je mov add cmp je xor cmp je sub add sub lea cmp je xor or sub xor and push pop mov sub add or lea push and push call push pop lea add and push call lea xor or lea push xor push call xor and cmp je add lea or xor test je xor mov and push lea push call push call lea mov add cmp je and mov push pop lea test je sub or sub push pop cmp je and xor sub test je mov lea or xor lea cmp je add xor or xor lea xor or xor add sub push pop add and mov xor lea and xor push pop lea push call xor add sub lea test je lea xor test je push pop and add cmp je lea push pop add xor and mov xor push call xor and push call xor sub xor sub push call leave retn push mov sub push call add xor sub push call sub lea push call push pop add lea leave retn push mov sub and push lea push call xor cmp je add lea mov add sub add lea test je sub lea test je test je lea and xor or xor lea xor lea xor push pop xor push pop lea sub push lea push lea push call xor push lea push call lea xor mov add sub or sub leave retn push mov sub lea and xor push pop sub xor and cmp je cmp je xor sub mov push lea push lea push call xor lea test je test je xor call lea add xor mov test je lea sub add xor push and push call mov sub xor cmp je cmp je xor push pop xor add or lea xor sub xor mov add test je cmp je xor push pop cmp je add push pop push pop and cmp je sub xor lea push pop cmp je test je xor sub push pop and sub xor sub mov or leave retn push mov sub push call or push call add lea cmp je xor add sub test je cmp je and xor sub xor lea or sub xor mov lea push call leave retn push mov sub lea push call xor test je test je and add lea sub push pop xor sub push call add xor lea add test je cmp je sub xor push call add and xor and test je test je xor mov lea mov test je add mov add test je and test je mov sub lea xor lea push call add mov push call sub push pop lea push call and push pop leave retn dw add xor retn jmp